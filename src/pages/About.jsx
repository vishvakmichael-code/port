export default function About() {
  return (
    <main style={{ minHeight: '100vh', paddingTop: 'var(--nav-height)' }}>
      <p style={{ color: 'var(--color-muted)', padding: 'var(--page-padding-x)' }}>
        About — content builds here
      </p>
    </main>
  )
}
